# privateGP

This project contains private code. The repository now includes a basic directory structure for application source code under `src/` and tests under `tests/`.

## Structure

- `src/` - application code
- `tests/` - unit tests

Additional placeholders are provided for future development.
