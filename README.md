# privateGP

This repository contains minimal example code with a simple test suite.

## Running Tests

Tests are executed with [pytest](https://docs.pytest.org/). Install the test
dependencies and run `pytest`:

```bash
pip install -r requirements.txt
pytest
```

Continuous integration is configured via GitHub Actions to run these tests on
every commit.

