# privateGP

This repository uses a simple structure to separate source code, tests, and documentation.

- `src/` contains the Python package. A minimal `__init__.py` is provided.
- `tests/` holds test modules; an empty `__init__.py` marks it as a package.
- `docs/` stores project documentation.
